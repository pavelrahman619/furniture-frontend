# Production Environment Configuration
# This file is used when NODE_ENV=production

# Backend API Configuration
# Update these URLs to your production backend domain
NEXT_PUBLIC_API_BASE_URL=https://furniture-ecom-backend.fly.dev
NEXT_PUBLIC_API_VERSION=api

# Environment
NODE_ENV=production

# Socket.IO Configuration (if using real-time features)
NEXT_PUBLIC_SOCKET_URL=https://furniture-ecom-backend.fly.dev

# Optional: Debug mode (set to false in production)
DEBUG_API=false
